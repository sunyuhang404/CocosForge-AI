# gameforge-agent

当前目录已重置为一个空壳 Koa 服务。

## Scripts

- `npm run dev`：开发模式启动服务。
- `npm run build`：编译 TypeScript 到 `dist/`。
- `npm start`：运行编译后的服务。
- `npm run lint`：仅做 TypeScript 类型检查。

## Endpoints

- `GET /`：服务基本信息。
- `GET /health`：健康检查。

